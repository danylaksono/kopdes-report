# Dependencies for reports/*/run.py
# pip install -r reports/requirements.txt

pandas>=2.0
duckdb>=1.0          # H3-parquet joins (03)
h3>=4.0              # k-rings, lat/lng -> cell (03, 04)
pyarrow>=15          # parquet writer
pyogrio>=0.7         # reads the Kontur GPKG once, to convert it (03)
rasterio>=1.3        # point-samples cloud COGs over HTTP range requests (04)
numpy>=1.24
scipy>=1.11          # cKDTree exact nearest-neighbour distances (10)
requests>=2.31       # live SIMKOPDES API (01)
pycryptodome>=3.20   # SIMKOPDES responses are AES-CBC encrypted (01)
